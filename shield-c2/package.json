{
  "name": "shield-c2",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Status + command-and-control dashboard for the NVIDIA Shield Docker host (unauthenticated, LAN-only).",
  "scripts": {
    "dev": "vite dev",
    "build": "vite build",
    "preview": "vite preview",
    "start": "node build"
  },
  "devDependencies": {
    "@sveltejs/adapter-node": "^5.5.4",
    "@sveltejs/kit": "^2.66.0",
    "@sveltejs/vite-plugin-svelte": "^6.0.0",
    "svelte": "^5.1.0",
    "vite": "^6.3.5"
  }
}

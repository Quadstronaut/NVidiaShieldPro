# --- dependencies / build output ---
node_modules/
.svelte-kit/
build/
dist/

# --- secrets (never commit) ---
*.env
deploy/deploy.env
*credentials*
*.token
*.pat
deploy/.last-deployed
# ssh deploy keys (the real key lives in /data/.ssh on the device, not here)
id_ed25519*
id_rsa*
shield_deploy_ed25519*
*.pem

# --- large binaries / blobs (kept out of git) ---
*.apk
*.apkm
*.zip
*.img
*.tgz
*.tar
*.tar.xz
docker-bringup/docker/
docker-bringup/bin/

# --- caches / logs / scratch ---
*.cache.json
*.log
.playwright-mcp/
.DS_Store

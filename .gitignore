# --- dependencies / build output ---
node_modules/
.svelte-kit/
build/
dist/
*.tsbuildinfo

# --- secrets (never commit) ---
*.env
.env
.env.*
!.env.example
deploy/deploy.env
docker-bringup/claude-term.env
docker-bringup/shield-access.env
*credentials*
*.token
*.pat
deploy/.last-deployed
# ssh deploy / host keys — real keys live on the device (/data/ssh, /data/.ssh), not here
id_ed25519*
id_rsa*
shield_deploy_ed25519*
*.pem
# Shield remote-access keys (born in the claude-home volume, never in-tree)
starhold_ed25519*
qflix_ed25519*
# tailscale node state + auth keys + gh token store (live under /data/tailscale + claude-home)
*authkey*
*.tskey
tailscaled.state
hosts.yml

# --- large binaries / device blobs (kept out of git) ---
*.apk
*.apkm
*.img
*.zip
*.tgz
*.deb
*.bin
*.tar
*.tar.gz
*.tar.xz
lineage-*.zip
recovery-*.img
docker-bringup/docker-24.0.9.tgz
docker-bringup/docker/
docker-bringup/bin/

# --- Claude scratch (keep council-ledger.jsonl tracked; ignore the rest) ---
.claude/worktrees/
.claude/_*
.superpowers/
# Design specs describe DEVIL's private topology (workstation paths, memory keys
# derived from the drive layout, private repo names). This repo is PUBLIC and its
# history is permanent — specs stay local. Redact before publishing any of them.
docs/superpowers/

# --- caches / logs / scratch ---
.playwright-mcp/
/*.png
review/
*.log
*.cache.json
.DS_Store

{
  "name": "claude-term",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "LAN-only phone-first native Claude Code UI for the NVIDIA Shield (headless-driven, not a terminal).",
  "scripts": {
    "start": "node server/index.js",
    "test": "node --test"
  },
  "dependencies": {
    "ws": "^8.18.0"
  }
}
